# 队友初版禁用参考代码

本目录保留队友初版的 `pid` 和 `mpu6050` 文件，仅供对照，不参与 `basic-1` 首版构建。

- 旧 `pid` 是单个编译期常量 PID，已由运行时 Q16.16 左右速度 PI 和循线 PD 取代。
- MPU6050 不是 `1-3` 首版闭环、安全或评分必需数据，因此不在上电路径读取或校准。
- `.disabled` 后缀防止 CCS 将它们当作在用 C 源码自动编译。

若后续启用 MPU6050 诊断通道，应新建符合中文模块注释、超时和非阻塞边界的独立模块，不应直接恢复这份初版文件。
